import { drawSquares } from "./graph";

// Input
const source = [2, 2];
const target = [4, 15];
const rows = 4;
const columns = 4;



drawSquares(rows, columns);
console.log('...drawSquares');
colorSquare(start[0], start[1], 'start');
colorSquare(target[0], target[1], 'target');


